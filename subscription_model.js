// JavaScript snippet for handling subscription logic
function subscribeUser(planId) {
  // Placeholder for subscription logic
  console.log(`Subscribing user to plan ${planId}`);
  // This would involve calling a backend service to handle the payment process and activate the subscription
}

// Example usage for a "premium" plan subscription button
document.getElementById('subscribePremium').addEventListener('click', function() {
  subscribeUser('premium');
});